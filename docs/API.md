# API

## `decline_flag(series)`

`1` iff the cell is `Снизилась`.

## `assign_segment(probability, profit, q=0.7)`

Independent q-quantiles. Labels: `high risk, high profit` and the other three combinations.
